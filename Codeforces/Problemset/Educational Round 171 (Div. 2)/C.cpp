#include <bits/stdc++.h>
using namespace std;

void solve() {
  int n;
  cin >> n;

  string s;
  cin >> s;

  auto check = [&](int x) -> bool {
    
  };

  int l = 0, r = n;
  while (l < r) {
    int m = (l + r) / 2;
    if ()
  }
  // cout << ans << '\n';
}

int main() {
  cin.tie(0)->sync_with_stdio(0);

  int tc;
  cin >> tc;
  while (tc--) {
    solve();
  }
}